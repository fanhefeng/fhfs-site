diff --git a/dist/events-156d8d12.esm.js b/dist/events-156d8d12.esm.js
index 9681d0c62238d67f2d5ad389307dfd45ee8ba7ba..5af6c569ec7931460c4a85cc79a5335ddeb9a7f7 100644
--- a/dist/events-156d8d12.esm.js
+++ b/dist/events-156d8d12.esm.js
@@ -1,6 +1,54 @@
 import * as React from 'react';
 import React__default from 'react';
 import * as THREE from 'three';
+
+/**
+ * fhfs-site patch — a clock with three's `Clock` semantics and none of its
+ * deprecation warning. three r183 marks `THREE.Clock` deprecated and logs
+ * from its constructor, and this file builds one per Canvas mount
+ * (pmndrs/react-three-fiber#3741, unresolved in 9.x). `THREE.Timer` is not a
+ * drop-in: it wants `update()` every frame, which this loop never calls, so
+ * the honest fix is the same class without the `warn()` line.
+ */
+class FiberClock {
+  constructor(autoStart = true) {
+    this.autoStart = autoStart;
+    this.startTime = 0;
+    this.oldTime = 0;
+    this.elapsedTime = 0;
+    this.running = false;
+  }
+  start() {
+    this.startTime = performance.now();
+    this.oldTime = this.startTime;
+    this.elapsedTime = 0;
+    this.running = true;
+  }
+  stop() {
+    this.getElapsedTime();
+    this.running = false;
+    this.autoStart = false;
+  }
+  getElapsedTime() {
+    this.getDelta();
+    return this.elapsedTime;
+  }
+  getDelta() {
+    let diff = 0;
+    if (this.autoStart && !this.running) {
+      this.start();
+      return 0;
+    }
+    if (this.running) {
+      const newTime = performance.now();
+      diff = (newTime - this.oldTime) / 1000;
+      this.oldTime = newTime;
+      this.elapsedTime += diff;
+    }
+    return diff;
+  }
+}
+
 import { createWithEqualityFn } from 'zustand/traditional';
 import { suspend, preload, clear } from 'suspend-react';
 import Tb, { unstable_getCurrentPriorityLevel, unstable_IdlePriority, unstable_LowPriority, unstable_NormalPriority, unstable_UserBlockingPriority, unstable_ImmediatePriority, unstable_scheduleCallback } from 'scheduler';
@@ -1013,7 +1061,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE.Clock(),
+      clock: new FiberClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/dist/events-4c71f21f.cjs.prod.js b/dist/events-4c71f21f.cjs.prod.js
index 65e702ef67d954387307b168529752690a586fd0..3345fd513cb5737e7837bf89675d8f153c22780c 100644
--- a/dist/events-4c71f21f.cjs.prod.js
+++ b/dist/events-4c71f21f.cjs.prod.js
@@ -2,6 +2,54 @@
 
 var React = require('react');
 var THREE = require('three');
+
+/**
+ * fhfs-site patch — a clock with three's `Clock` semantics and none of its
+ * deprecation warning. three r183 marks `THREE.Clock` deprecated and logs
+ * from its constructor, and this file builds one per Canvas mount
+ * (pmndrs/react-three-fiber#3741, unresolved in 9.x). `THREE.Timer` is not a
+ * drop-in: it wants `update()` every frame, which this loop never calls, so
+ * the honest fix is the same class without the `warn()` line.
+ */
+class FiberClock {
+  constructor(autoStart = true) {
+    this.autoStart = autoStart;
+    this.startTime = 0;
+    this.oldTime = 0;
+    this.elapsedTime = 0;
+    this.running = false;
+  }
+  start() {
+    this.startTime = performance.now();
+    this.oldTime = this.startTime;
+    this.elapsedTime = 0;
+    this.running = true;
+  }
+  stop() {
+    this.getElapsedTime();
+    this.running = false;
+    this.autoStart = false;
+  }
+  getElapsedTime() {
+    this.getDelta();
+    return this.elapsedTime;
+  }
+  getDelta() {
+    let diff = 0;
+    if (this.autoStart && !this.running) {
+      this.start();
+      return 0;
+    }
+    if (this.running) {
+      const newTime = performance.now();
+      diff = (newTime - this.oldTime) / 1000;
+      this.oldTime = newTime;
+      this.elapsedTime += diff;
+    }
+    return diff;
+  }
+}
+
 var traditional = require('zustand/traditional');
 var suspendReact = require('suspend-react');
 var Tb = require('scheduler');
@@ -1038,7 +1086,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: new FiberClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/dist/events-b1bdeb1a.cjs.dev.js b/dist/events-b1bdeb1a.cjs.dev.js
index 7785529f79299cdefe706c0df1d5c756817ca274..2c9f11a39279c16d06006f81a86ee818026f2372 100644
--- a/dist/events-b1bdeb1a.cjs.dev.js
+++ b/dist/events-b1bdeb1a.cjs.dev.js
@@ -2,6 +2,54 @@
 
 var React = require('react');
 var THREE = require('three');
+
+/**
+ * fhfs-site patch — a clock with three's `Clock` semantics and none of its
+ * deprecation warning. three r183 marks `THREE.Clock` deprecated and logs
+ * from its constructor, and this file builds one per Canvas mount
+ * (pmndrs/react-three-fiber#3741, unresolved in 9.x). `THREE.Timer` is not a
+ * drop-in: it wants `update()` every frame, which this loop never calls, so
+ * the honest fix is the same class without the `warn()` line.
+ */
+class FiberClock {
+  constructor(autoStart = true) {
+    this.autoStart = autoStart;
+    this.startTime = 0;
+    this.oldTime = 0;
+    this.elapsedTime = 0;
+    this.running = false;
+  }
+  start() {
+    this.startTime = performance.now();
+    this.oldTime = this.startTime;
+    this.elapsedTime = 0;
+    this.running = true;
+  }
+  stop() {
+    this.getElapsedTime();
+    this.running = false;
+    this.autoStart = false;
+  }
+  getElapsedTime() {
+    this.getDelta();
+    return this.elapsedTime;
+  }
+  getDelta() {
+    let diff = 0;
+    if (this.autoStart && !this.running) {
+      this.start();
+      return 0;
+    }
+    if (this.running) {
+      const newTime = performance.now();
+      diff = (newTime - this.oldTime) / 1000;
+      this.oldTime = newTime;
+      this.elapsedTime += diff;
+    }
+    return diff;
+  }
+}
+
 var traditional = require('zustand/traditional');
 var suspendReact = require('suspend-react');
 var Tb = require('scheduler');
@@ -1038,7 +1086,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: new FiberClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
